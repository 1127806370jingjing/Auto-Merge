J-Flash Path 路径设置和.jflash文件路径设置，隐藏在一个折叠的二级菜单中 不显示在主页面。
主页只保留 三个输入框，一个按钮。
输入框分别对应BOOT路径、APP文件路径（两个用来merge的bin文件，一个用于boot，一个用于app）。还有一个输入框为Merge过后的hex文件路径。
按钮对应执行。
执行按钮执行后，会自动执行merge操作，并执行操作。

需要有合并按钮，J-Flash Path 路径设置需要自动扫盘查找J-Flash.exe路径。


现在Merge操作已经完成
我现在需要能够批量Merge的功能拖入一个Boot文件夹和一个App文件夹，然后自动Merge成HEX文件，自动识别文件名（抓取关键子重复字样，匹配boot和app），自动Merge成HEX文件。

1.已实现Merge功能（单一Merge和批量Merge），我需要一个能指定合并后的HEX文件路径的输入框。
2.自动匹配列表后，我需要给每个自动匹配列表的HEX文件名前添加一个数字，数字为物料编号（我手动填写“22110510_+文件名.hex”）
3.重新调整一下UI布局，目前的UI无法自动根据拖拽调整UI布局。

1.我希望在单个文件Merge的时候也能复用相同的逻辑，自动识别文件名（抓取关键子重复字样，匹配boot和app，匹配不成功提示不成功不许Merge），命名方式也和批量Merge一样，添加一个数字，数字为物料编号（我手动填写“22110510_+文件名.hex”）


1. 我需要一个功能，能够帮我确认MCU的擦除与烧录HEX文件的操作。同样使用J-Flash.exe进行操作，具体为擦除（Erase Chip）与烧录HEX文件（Production Programming）的操作。

擦除烧录执行的Jflash操作为：Connect → Erase Chip → Production Programming → Exit。

并且添加状态提示，若连接失败，提示“未连接成功JFLASH”。

这是一个单独的功能，单文件和批量Merge都可使用该功能。
我需要使用这个功能验证Merge后的HEX文件能否正常烧录。

2. 烧录后可以使用HID进行通信，获取PID和VID并记录显示在UI上。


1.根据烧录后的HEX文件信息（提取关键词，0x开头的16进制数字），在HID列表上找到对应的设备，并高亮显示在UI上。
2.我需要获取到设备的版本号，并记录显示在HID列表上，获取版本号的指令为02 96 00 ,发送后返回格式为，02 96 00 00 00 13 91，13 91表示版本号V13.91








